import { notFound } from 'next/navigation'; import Header from '@/components/Header'; import Footer from '@/components/Footer'; import { getArticleBySlug, articles } from '@/lib/articles-data'; import Link from 'next/link';
export function generateStaticParams(){return articles.map(a=>({slug:a.slug}))}
export default async function Article({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=getArticleBySlug(slug);if(!a)notFound();return <div className="min-h-screen bg-[#f5f1e8]"><Header/><main><article><section className="bg-[#0b1714] px-5 pb-16 pt-36 text-white"><div className="mx-auto max-w-4xl"><Link href="/artikel" className="text-sm text-white/50">← Semua artikel</Link><div className="mt-8 text-xs font-bold uppercase tracking-[.22em] text-[#d6bd8a]">{a.category} • {new Date(a.date).toLocaleDateString('id-ID',{dateStyle:'long'})}</div><h1 className="mt-4 text-4xl font-medium leading-tight md:text-6xl">{a.title}</h1><p className="mt-6 text-lg leading-8 text-white/60">{a.excerpt}</p></div></section><div className="mx-auto max-w-4xl px-5 py-16"><img src={a.thumbnail} alt={a.title} className="mb-12 w-full rounded-[2rem] bg-[#10211c]"/><div className="article-content text-lg leading-8 text-[#10211c]/75" dangerouslySetInnerHTML={{__html:a.content}}/></div></article></main><Footer/></div>}
